# =============================================================
# Preis-Update alle 30 Minuten via Tankerkönig API
# 48 Standorte in 24 Gruppen (Rotation), 2 Aufrufe pro Lauf
# 48 Runs/Tag × 2 = 96 API-Aufrufe/Tag (Limit: 100)
# Committed die neuen Daten → triggert automatisch deploy.yml
# =============================================================

name: Update Fuel Prices

on:
  schedule:
    # Alle 30 Minuten: Minute 7 und 37 (vermeidet :00/:30 Rush)
    - cron: '7,37 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: price-update
  cancel-in-progress: true

jobs:
  fetch-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Tankstellenpreise abrufen
        run: node scripts/fetch-prices.mjs
        env:
          TANKERKOENIG_API_KEY: ${{ secrets.VITE_TANKERKOENIG_API_KEY }}

      - name: Preis-Daten committen und pushen
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/stations.json
          # Nur committen wenn sich Daten geändert haben
          if git diff --cached --quiet; then
            echo "Keine Änderungen, überspringe Commit"
          else
            git commit -m "chore: Preise aktualisiert — Gruppe $(date -u +'%H:%M UTC') [$(date -u +'%Y-%m-%d')]"
            git push
          fi
